<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base de Material 3 con soporte DayNight -->
    <style name="Base.Theme.ProCard" parent="Theme.Material3.DayNight">
        <!-- Quita la action bar del sistema -->
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <!-- Si usas AppCompatActivity y quieres también quitar la Toolbar de AppCompat,
             puedes añadir (opcional) cuando tengas appcompat:
             <item name="windowActionBar">false</item>
        -->
    </style>

    <!-- Tema público que usarás en el Manifest -->
    <style name="Theme.ProCard" parent="Base.Theme.ProCard" />

</resources>
